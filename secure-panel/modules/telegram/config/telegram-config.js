/**
 * Client-side Telegram integration is intentionally disabled.
 * Bot credentials must be stored only in a trusted server-side environment.
 */
export const telegramConfig = Object.freeze({
  enabled: false,
  transport: null
});
