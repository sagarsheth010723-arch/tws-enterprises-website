(()=>{const body=document.body,menu=document.getElementById('oneMobileMenu'),toggle=document.getElementById('oneMenuToggle');function setMenu(open){if(!menu)return;menu.classList.toggle('open',open);menu.setAttribute('aria-hidden',String(!open));body.classList.toggle('menu-open',open)}toggle?.addEventListener('click',()=>setMenu(true));document.querySelectorAll('[data-one-close]').forEach(x=>x.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});document.querySelectorAll('.one-mobile-scroll details').forEach(d=>d.addEventListener('toggle',()=>{const s=d.querySelector('summary span');if(s)s.textContent=d.open?'−':'+'}));const io='IntersectionObserver'in window?new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.08}):null;document.querySelectorAll('.reveal').forEach((el,i)=>{el.style.transitionDelay=Math.min(i%5*.06,.24)+'s';io?io.observe(el):el.classList.add('visible')});const header=document.getElementById('oneHeader');let last=0;window.addEventListener('scroll',()=>{const y=window.scrollY;header?.classList.toggle('scrolled',y>30);last=y},{passive:true});document.querySelectorAll('[data-one-form]').forEach(form=>{form.addEventListener('submit',async e=>{e.preventDefault();const status=form.querySelector('[data-form-status]'),btn=form.querySelector('button[type=submit]'),original=btn.textContent;status.className='one-form-status';btn.disabled=true;btn.textContent='Submitting…';try{const r=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});if(!r.ok)throw new Error('Submission failed');status.textContent='Project enquiry submitted successfully. Redirecting…';status.classList.add('show','success');form.reset();setTimeout(()=>location.href='thank-you.html',900)}catch(err){status.textContent='The form could not be submitted. Please try again or contact us on WhatsApp.';status.classList.add('show','error');btn.disabled=false;btn.textContent=original}})});})();